# Scripting-for-Cybersecurity-
This project uses PowerShell and Python to automate security. PowerShell captures TCP connections to a CSV. Python then scans external IPs via the AbuseIPDB API. The final JSON report speeds up the incident response for security analysts
